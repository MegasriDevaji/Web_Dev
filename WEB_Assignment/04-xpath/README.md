# XPath

This assignment uses a BookHub XML file and multiple XPath expressions.

## Files

- `bookhub-xpath.xml` - XML document
- `xpath-queries.txt` - XPath expressions
- `expected-results.txt` - expected output

## What it shows

- Node selection
- Filtering with conditions
- Attribute-based selection
- Numeric comparison
- XPath functions like `last()`
