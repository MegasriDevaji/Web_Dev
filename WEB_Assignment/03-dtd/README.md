# DTD

This assignment uses an external DTD to define the structure of a BookHub XML document.

## Files

- `bookhub-dtd.xml` - XML document
- `bookhub-dtd.dtd` - DTD definition
- `notes.txt` - short explanation

## What it shows

- External DTD usage
- XML validation structure
- Required ID attribute
- Repeated book elements
